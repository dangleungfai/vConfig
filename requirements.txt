# 系统依赖：必须安装 OpenSSL（用于 HTTPS 自签名证书）
# 见 README 部署步骤 - 环境要求
Flask>=2.3.0
Flask-SQLAlchemy>=3.0.0
Pillow>=9.0.0
APScheduler>=3.10.0
tzdata>=2023.0; sys_platform == "win32"
Werkzeug>=2.3.0
gunicorn>=21.0.0
paramiko>=3.0.0
ldap3>=2.9.0
# MariaDB/MySQL 驱动（使用 MariaDB 时需设置 DATABASE_URL 或 MARIADB_* 环境变量）
PyMySQL>=1.1.0
# 自动发现依赖 SNMP 客户端库 pysnmp（服务端从设备取 hostname / sysDescr）
# 旧版 pysnmp 4.x 使用 imp 模块，与 Python 3.12 不兼容，这里升级到 7.x
pysnmp>=7.0.0
pyasn1>=0.4.8
pyasn1-modules>=0.2.8